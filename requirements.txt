# NumPy 2.0 移除了 np.NaN，pyannote.audio 內部仍使用舊 API，需釘住 1.x
numpy<2.0

# 語音辨識（含字詞級時間戳對齊）
whisperx

# 說話人分割
# 3.3.2：修復了 3.1.1 的 tensor size mismatch bug（特定音訊長度會觸發）
# torchvision 循環 import 問題已由 Dockerfile 預先安裝 torchvision==0.20.1 解決
pyannote.audio==3.3.2

# huggingface_hub >= 0.23.0 移除了 use_auth_token 參數，
# 但 pyannote.audio 3.3.2 內部仍以 use_auth_token 呼叫 hf_hub_download()，
# 需釘住舊版以維持相容性。
huggingface_hub<0.23.0

# faster-whisper 的隱性依賴（未列在其 setup 中）
requests>=2.28.0

# 資料夾監控
watchdog>=3.0.0

# 設定檔
pyyaml>=6.0

# 進度條
tqdm>=4.65.0
